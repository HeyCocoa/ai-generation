spring:
  application:
    name: ai-generation
server:
  port: 8080
  servlet:
    context-path: /api
springdoc:
  group-configs:
    - group: 'default'
      packages-to-scan: org.example.aigeneration.controller
knife4j:
  enable: true
  setting:
    language: zh_cn

